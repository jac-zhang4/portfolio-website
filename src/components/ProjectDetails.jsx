import React from "react";

const ProjectDetails = ({ title, description, image, tags, href, closeModal }) => {
  return (
    <div className="project-details">
      {/* Project details content will be added here */}
    </div>
  );
};

export default ProjectDetails;
